pipeline{
    agent any
    stages{
        stage("Verify tooling"){
            steps{
                bat '''
                docker build -t composetest .
                docker compose up
                '''
            }
        }
    }
}