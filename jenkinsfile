pipeline{
    agent any
    stages{
        stage("Verify tooling"){
            steps{
                bat encoding: 'utf-8', returnStdout: true, script: 'docker compose up'
            }
        }
    }
}