pipeline{
    agent any
    stages{
        stage("Building"){
            bat "docker build -t nodeapp ."
        }
        stage("Compose up"){
            steps{
                bat "docker nodeapp run"
            }
        }
    }
}